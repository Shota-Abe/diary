import 'package:flutter/material.dart';

class AddElementDictionary extends StatelessWidget {
  //図鑑の要素を追加するページ

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('アクティビティを追加')));
  }
}
